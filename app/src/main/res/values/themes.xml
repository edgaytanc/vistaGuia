<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema principal -->
    <style name="Theme.EtiquetadoDeImagenMLKotlin" parent="Theme.MaterialComponents.DayNight">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Personalización de componentes -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <!-- Estilo personalizado para botones -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:insetTop">12dp</item>
        <item name="android:insetBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="fontFamily">@font/kanit_bold</item>
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">8dp</item>
    </style>
</resources>