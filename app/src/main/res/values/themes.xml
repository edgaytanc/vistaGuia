<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal para Views (Material 3, sin ActionBar) -->
    <style name="Theme.VistaMedMVP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Opcional: status bar transparente -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Opcional: navegación oscura -->
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Forzar edge-to-edge si deseas -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
